{
  "name": "apps-script-x-power-tracker",
  "version": "1.0.1",
  "scripts": {
    "build": "node build",
    "watch": "node build --watch",
    "push": "node build && clasp push",
    "push:debug": "node build -- --debug --no-minify && clasp push"
  },
  "author": {
    "name": "yukidaruma",
    "url": "https://github.com/yukidaruma"
  },
  "license": "MIT",
  "dependencies": {
    "@splatoon-stats/types": "^1.0.2"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.31",
    "dotenv": "^9.0.1",
    "esbuild": "^0.11.10"
  }
}
